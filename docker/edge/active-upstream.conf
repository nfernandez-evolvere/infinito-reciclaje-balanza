# Upstream ACTIVO — lo lee el edge nginx. Arranca apuntando a BLUE.
# deploy.sh lo reescribe en cada cutover (copiando upstream-blue|green.conf).
# No editar a mano en producción: lo gestiona el script de deploy.
upstream balanza_app {
    server balanza-app-blue:8080 max_fails=3 fail_timeout=10s;
}
