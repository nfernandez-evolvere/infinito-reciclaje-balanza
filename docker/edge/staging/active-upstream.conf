# Upstream ACTIVO de STAGING — lo gestiona deploy.sh, no editar a mano.
# Placeholder de arranque: staging aún NO desplegado. Permite que el edge levante
# antes del primer deploy (ver nota en prod/active-upstream.conf). El primer deploy
# de staging reemplaza esta línea por el upstream real (blue/green).
upstream balanza_staging_app {
    server 127.0.0.1:9;
}
