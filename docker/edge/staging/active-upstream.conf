# Upstream ACTIVO de STAGING — lo gestiona deploy.sh, no editar a mano.
# Arranca apuntando a blue. deploy.sh lo reescribe en cada cutover.
upstream balanza_staging_app {
    server balanza-staging-app-blue:8080 max_fails=3 fail_timeout=10s;
}
