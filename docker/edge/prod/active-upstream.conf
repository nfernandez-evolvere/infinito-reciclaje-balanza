# Upstream ACTIVO de PRODUCCIÓN — lo gestiona deploy.sh, no editar a mano.
# Arranca apuntando a blue. deploy.sh lo reescribe en cada cutover.
upstream balanza_prod_app {
    server balanza-prod-app-blue:8080 max_fails=3 fail_timeout=10s;
}
