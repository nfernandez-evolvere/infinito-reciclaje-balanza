# Upstream → color BLUE. deploy.sh copia este archivo sobre active-upstream.conf
# para conmutar el tráfico a blue.
upstream balanza_app {
    server balanza-app-blue:8080 max_fails=3 fail_timeout=10s;
}
