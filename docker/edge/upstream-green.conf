# Upstream → color GREEN. deploy.sh copia este archivo sobre active-upstream.conf
# para conmutar el tráfico a green.
upstream balanza_app {
    server balanza-app-green:8080 max_fails=3 fail_timeout=10s;
}
